using System.Threading;
using System.Threading.Tasks;

namespace WebApolice.Modulos.Seguro.Application.Ports;

public interface IApolicesQueries
{
    // A ser expandido na Fase 2 com DTOs e métodos específicos
    // Task<(IEnumerable<ApoliceListagemItemResult> Itens, int TotalItens, int TotalPaginas)> ListarPaginadoAsync(...);
}
